#include "processo.cpp"

class Escalonador
{
private:
    
public:
    Escalonador(/* args */);
    ~Escalonador();
    void escalonar(std::vector<Processo> processos);
};

void escalonar(std::vector<Processo> processos)
{
    
}

